# Some cmd line commands that are nice to have and some required

```bash
neofetch

tree

htop

gtop (if you want something more graphic)

hwloc
```

# Update / Upgrade Pop (in this order)

```bash
sudo apt install update

sudo apt install upgrade
```

# log into sudo

```bash
sudo -i
```
